#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include "func.h"  // Inclui o arquivo de cabeçalho que contém as funções do jogo

int main() {
    menu();  // Chamada para a função menu
    return 0;
}